<UserControl 
    x:Class="ElectronicCad.Desktop.Styles.Components.InputControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d" 
    xmlns:local="clr-namespace:ElectronicCad.Desktop.Styles.Components"
    x:Name="Current">
    <StackPanel>
        <Label 
            Margin="0 0 0 5"
            Foreground="White" 
            Target="{Binding ElementName=TextBox}" 
            Content="{Binding Title, ElementName=Current}"/>
        
        <TextBox x:Name="TextBox" Text="{Binding Value, ElementName=Current, UpdateSourceTrigger=LostFocus}" />
  </StackPanel>
</UserControl>
