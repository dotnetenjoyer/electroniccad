<UserControl 
    x:Class="ElectronicCad.Desktop.Views.Properties.Properties"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d"
    xmlns:properties="clr-namespace:ElectronicCad.MVVM.ViewModels.Properties;assembly=ElectronicCad.MVVM"
    d:DataContext="{d:DesignInstance Type=properties:PropertyViewModel}"
    Background="{StaticResource SecondaryBackgroundBrush}">
    <Grid>
        <ListView ItemsSource="{Binding PropertyObject.Properties}">
            <ListView.ItemTemplate>
                <ItemContainerTemplate>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="{Binding Name}"/>
                        <TextBox/>
                    </StackPanel>
                </ItemContainerTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
